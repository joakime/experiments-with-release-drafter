name: Release Drafter (9.4.x)
on:
  push:
    branches:
      - jetty-9.4.x
jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v5.11.0
        with:
          name: "9.4.x"
          version: "9.4.32"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
